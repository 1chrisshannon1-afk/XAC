# CI Hooks

Project-specific hook extensions. The shared hooks live in `_XAC_Base/ci/hooks/`.

| File | Purpose |
|------|---------|
| `pre-commit-extra.ps1` | Extra pre-commit checks (sourced by `_XAC_Base/ci/hooks/pre-commit.ps1`) |
